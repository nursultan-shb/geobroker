# server information
[server]
brokerId = "broker"
port = 5559
granularity = 5
messageProcessors = 1
planCreatorAddress = "tcp://127.0.0.1:7001"
loadBalancerAddress = "tcp://127.0.0.1:5559"

    # server mode specific information (only one should be uncommented)
#    [server.mode]
#    name = "single"

    [server.mode]
    name = "disgb_subscriberMatching"
    brokerAreaFilePath = "src/test/resources/connect_and_disconnect.json"
    brokerCommunicators = 1

